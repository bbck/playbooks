---

ses_access_key: "{{ vault_ses_access_key }}"
ses_secret_key: "{{ vault_ses_secret_key }}"

gitlab_workhorse_secret_token: "{{ vault_gitlab_workhorse_secret_token }}"
gitlab_shell_secret_token: "{{ vault_gitlab_shell_secret_token }}"
gitlab_rails_secret_key_base: "{{ vault_gitlab_rails_secret_key_base }}"
gitlab_rails_db_key_base: "{{ vault_gitlab_rails_db_key_base }}"
gitlab_rails_otp_key_base: "{{ vault_gitlab_rails_otp_key_base }}"
registry_http_secret: "{{ vault_registry_http_secret }}"
registry_internal_certificate: "{{ vault_registry_internal_certificate }}"
registry_internal_key: "{{ vault_registry_internal_key }}"
mattermost_email_invite_salt: "{{ vault_mattermost_email_invite_salt }}"
mattermost_file_public_link_salt: "{{ vault_mattermost_file_public_link_salt }}"
mattermost_email_password_reset_salt: "{{ vault_mattermost_email_password_reset_salt }}"
mattermost_sql_at_rest_encrypt_key: "{{ vault_mattermost_sql_at_rest_encrypt_key }}"
